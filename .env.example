# No API keys or environment variables are required.
# The app runs entirely on keyless OpenStreetMap APIs:
#   - Map display     → OpenStreetMap tiles via Leaflet
#   - Location search → Nominatim (OpenStreetMap)
#   - Streets & buildings → Overpass API (OpenStreetMap)
# Terrain relief is generated procedurally (no elevation provider).
